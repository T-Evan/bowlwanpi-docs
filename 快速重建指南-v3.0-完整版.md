# 快速重建指南 v3.0 完整版

更新日期: 2026-02-17

**5分钟快速恢复 BowlWanpi 完整能力**

---

## 写在前面

本指南适用于：
- 系统崩溃后的快速重建
- 迁移到新服务器
- 复制当前环境到新实例
- 灾难恢复

**前提条件**: 已有备份文件或原服务器可访问

---

## 1分钟 - 基础环境检查

### 快速检查清单

```bash
# 执行快速检查脚本
curl -fsSL https://raw.githubusercontent.com/yourrepo/bowlwanpi/main/quick-check.sh | bash

# 或手动检查
node --version      # 需要 v22+
python3.11 --version # 需要 3.11+
mihomo --version    # 需要已安装
```

### 如果缺少依赖

```bash
# 一键安装依赖
./scripts/install-dependencies.sh

# 或手动安装
nvm install 22
nvm use 22
npm install -g openclaw
```

---

## 2分钟 - 恢复工作区

### 方式一：从备份恢复（推荐）

```bash
# 如果你有备份文件
BACKUP_FILE="/clawd-data/workspace-backup/20260217-120000/workspace.tar.gz"

# 解压到工作区
mkdir -p ~/.openclaw/workspace
tar -xzf $BACKUP_FILE -C ~/.openclaw/

# 恢复凭证（从加密备份）
tar -xzf /clawd-data/workspace-backup/20260217-120000/secrets.tar.gz -C ~/.openclaw/workspace/
```

### 方式二：从Git仓库克隆

```bash
# 克隆工作区仓库
git clone https://github.com/yourusername/bowlwanpi-workspace.git ~/.openclaw/workspace

# 恢复凭证文件（手动复制或从安全位置下载）
# 将凭证文件复制到 ~/.openclaw/workspace/secrets/
```

### 方式三：从原服务器复制

```bash
# 使用rsync从原服务器复制
rsync -avz --progress root@old-server:/root/.openclaw/workspace/ ~/.openclaw/workspace/

# 或使用scp
scp -r root@old-server:/root/.openclaw/workspace/* ~/.openclaw/workspace/
```

---

## 3分钟 - 配置验证

### 自动验证脚本

```bash
#!/bin/bash
# 保存为 verify-rebuild.sh

echo "🥣 BowlWanpi 重建验证"
echo "======================"

ERRORS=0

# 1. 检查Node.js
echo -n "[1/8] Node.js... "
if node --version | grep -q "v22"; then
  echo "✅"
else
  echo "❌ 需要Node.js v22+"
  ((ERRORS++))
fi

# 2. 检查Python
echo -n "[2/8] Python... "
if python3.11 --version | grep -q "3.11"; then
  echo "✅"
else
  echo "❌ 需要Python 3.11+"
  ((ERRORS++))
fi

# 3. 检查工作区
echo -n "[3/8] 工作区... "
if [ -d ~/.openclaw/workspace ]; then
  echo "✅"
else
  echo "❌ 工作区不存在"
  ((ERRORS++))
fi

# 4. 检查凭证
echo -n "[4/8] 凭证文件... "
if [ -f ~/.openclaw/workspace/secrets/memu-credentials.json ]; then
  echo "✅"
else
  echo "❌ 缺少凭证文件"
  ((ERRORS++))
fi

# 5. 检查Mihomo
echo -n "[5/8] Mihomo... "
if systemctl is-active --quiet mihomo; then
  echo "✅"
else
  echo "⚠️  未运行 (将自动启动)"
fi

# 6. 检查OpenClaw配置
echo -n "[6/8] OpenClaw配置... "
if openclaw config validate; then
  echo "✅"
else
  echo "❌ 配置错误"
  ((ERRORS++))
fi

# 7. 检查技能
echo -n "[7/8] 技能... "
SKILL_COUNT=$(clawhub list 2>/dev/null | wc -l)
if [ $SKILL_COUNT -ge 10 ]; then
  echo "✅ ($SKILL_COUNT个)"
else
  echo "⚠️  技能较少 ($SKILL_COUNT个)"
fi

# 8. 检查定时任务
echo -n "[8/8] 定时任务... "
if [ -f /etc/cron.d/bowlwanpi-backup-cron ]; then
  echo "✅"
else
  echo "⚠️  未配置"
fi

echo "======================"
if [ $ERRORS -eq 0 ]; then
  echo "✅ 验证通过！准备启动..."
else
  echo "❌ 发现 $ERRORS 个问题，请修复后再启动"
  exit 1
fi
```

### 手动验证

```bash
# 检查关键文件是否存在
ls ~/.openclaw/openclaw.json
ls ~/.openclaw/workspace/mcp_config.json
ls ~/.openclaw/workspace/secrets/*.json

# 验证配置格式
openclaw config validate
python3 -m json.tool ~/.openclaw/workspace/mcp_config.json > /dev/null && echo "MCP配置OK"
```

---

## 4分钟 - 启动服务

### 一键启动脚本

```bash
#!/bin/bash
# 保存为 start-all.sh

echo "🚀 启动 BowlWanpi 服务..."

# 1. 启动Mihomo
echo "[1/4] 启动Mihomo..."
sudo systemctl start mihomo
sleep 2
if systemctl is-active --quiet mihomo; then
  echo "  ✅ Mihomo已启动"
else
  echo "  ❌ Mihomo启动失败"
  exit 1
fi

# 2. 设置代理环境变量
echo "[2/4] 设置代理..."
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
echo "  ✅ 代理已设置"

# 3. 验证代理
echo "[3/4] 验证代理..."
if curl -x http://127.0.0.1:7890 -s -o /dev/null -w "%{http_code}" https://www.google.com | grep -q "200"; then
  echo "  ✅ 代理连接正常"
else
  echo "  ⚠️  代理连接异常，但继续启动..."
fi

# 4. 启动OpenClaw
echo "[4/4] 启动OpenClaw..."
openclaw gateway start
sleep 2
if openclaw gateway status | grep -q "running"; then
  echo "  ✅ OpenClaw已启动"
else
  echo "  ❌ OpenClaw启动失败"
  exit 1
fi

echo ""
echo "🎉 所有服务已启动！"
echo ""
echo "验证命令:"
echo "  openclaw gateway status"
echo "  openclaw mcp list"
echo "  openclaw cron list"
```

### 手动启动

```bash
# 1. 启动Mihomo
sudo systemctl start mihomo

# 2. 设置代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890

# 3. 启动OpenClaw
openclaw gateway start

# 4. 验证
openclaw gateway status
```

---

## 5分钟 - 功能测试

### 快速测试清单

```bash
# 测试1: 基础功能
echo "测试1: 基础功能"
openclaw gateway status && echo "✅ 网关正常"

# 测试2: MCP服务
echo "测试2: MCP服务"
openclaw mcp list | grep -q "bowlwanpi" && echo "✅ MCP正常"

# 测试3: 智能搜索
echo "测试3: 智能搜索"
python3 -c "
from skills.intelligent-search.search_engine import smart_search
import asyncio
result = asyncio.run(smart_search('OpenClaw', limit=1))
print('✅ 搜索正常' if result else '❌ 搜索失败')
"

# 测试4: 记忆系统
echo "测试4: 记忆系统"
python3 -c "
from skills.unified-memory.unified_memory_manager import retrieve_merged
import asyncio
result = asyncio.run(retrieve_merged('测试', limit=1))
print('✅ 记忆系统正常')
"

# 测试5: 网页抓取
echo "测试5: 网页抓取"
python3 skills/crawl-for-ai/scripts/crawl_page.py "https://www.baidu.com" > /tmp/test-crawl.md && echo "✅ 抓取正常"

# 测试6: 飞书连接 (如果配置了)
echo "测试6: 飞书连接"
# 发送测试消息到飞书
openclaw message send --channel feishu --target "你的用户ID" --message "🥣 BowlWanpi 重建完成！"
```

---

## 一键重建脚本

### 完整重建脚本

```bash
#!/bin/bash
# 保存为 rebuild.sh
# 一键重建 BowlWanpi

set -e  # 遇到错误立即退出

echo "🥣 BowlWanpi 一键重建"
echo "======================"

# 配置
BACKUP_DIR="${1:-/clawd-data/workspace-backup/latest}"
SKIP_DEPS="${2:-false}"

# 1. 安装依赖
if [ "$SKIP_DEPS" != "true" ]; then
  echo "[1/6] 安装依赖..."
  ./scripts/install-dependencies.sh
fi

# 2. 恢复工作区
echo "[2/6] 恢复工作区..."
if [ -d "$BACKUP_DIR" ]; then
  ./scripts/restore-workspace.sh "$BACKUP_DIR"
else
  echo "⚠️  未找到备份，创建新工作区..."
  mkdir -p ~/.openclaw/workspace/{memory,scripts,secrets,skills,cron,.learnings,docs}
fi

# 3. 验证配置
echo "[3/6] 验证配置..."
./scripts/verify-rebuild.sh || exit 1

# 4. 启动服务
echo "[4/6] 启动服务..."
./scripts/start-all.sh || exit 1

# 5. 安装技能
echo "[5/6] 安装技能..."
clawhub install notify session-cost self-improving-agent crawl-for-ai faster-whisper bilibili-hot-monitor hacker-news ttrpg-gm summarize task-status weather openclaw-cost-guard

# 6. 功能测试
echo "[6/6] 功能测试..."
./scripts/test-functions.sh

echo ""
echo "======================"
echo "🎉 重建完成！"
echo ""
echo "后续步骤:"
echo "1. 检查飞书连接是否正常"
echo "2. 验证定时任务是否运行"
echo "3. 测试记忆系统同步"
echo "4. 发送测试消息确认"
```

### 使用方式

```bash
# 从最新备份重建
./rebuild.sh

# 从指定备份重建
./rebuild.sh /clawd-data/workspace-backup/20260217-120000

# 跳过依赖安装（如果已安装）
./rebuild.sh /clawd-data/workspace-backup/latest true
```

---

## 关键信息速查

### 重要路径

| 路径 | 说明 |
|------|------|
| `~/.openclaw/workspace/` | 工作区主目录 |
| `~/.openclaw/openclaw.json` | OpenClaw主配置 |
| `~/.openclaw/workspace/secrets/` | 凭证文件目录 |
| `/etc/mihomo/config.yaml` | Mihomo代理配置 |
| `/etc/cron.d/bowlwanpi-backup-cron` | 定时任务配置 |
| `/var/log/bowlwanpi-*.log` | 日志文件 |
| `/clawd-data/workspace-backup/` | 备份目录 |

### 关键凭证

| 凭证 | 位置 | 用途 |
|------|------|------|
| memU API Key | `secrets/memu-credentials.json` | 云端记忆 |
| 腾讯云 Secret | `secrets/tencent-credentials.json` | 搜索服务 |
| Moltbook API Key | `secrets/moltbook-credentials.json` | AI社区 |
| Tavily API Key | `secrets/tavily-credentials.env` | 搜索服务 |
| 豆包 Token | `skills/doubao-tts/.env` | 语音合成 |

### 快速命令

```bash
# 状态检查
openclaw gateway status          # 网关状态
openclaw mcp list               # MCP服务
openclaw cron list              # 定时任务
sudo systemctl status mihomo    # 代理状态

# 日志查看
tail -f /var/log/bowlwanpi-heartbeat.log    # 心跳日志
tail -f /var/log/bowlwanpi-nightly-build.log # 构建日志
tail -f /var/log/bowlwanpi-cron.log         # 定时任务日志

# 重启服务
openclaw gateway restart        # 重启网关
sudo systemctl restart mihomo   # 重启代理
sudo systemctl restart crond    # 重启定时任务

# 备份
./scripts/backup.sh             # 创建备份
```

---

## 故障速查

### 常见问题快速解决

| 问题 | 快速解决 |
|------|----------|
| **服务启动失败** | `openclaw gateway status` 查看错误日志 |
| **代理不通** | `sudo systemctl restart mihomo` 重启代理 |
| **技能丢失** | `clawhub list` 查看，重新安装缺失技能 |
| **记忆不同步** | 检查 `secrets/memu-credentials.json` 是否有效 |
| **飞书收不到消息** | 检查 `openclaw.json` 飞书配置是否正确 |
| **定时任务不执行** | `sudo cat /var/log/cron | grep bowlwanpi` 查看日志 |

### 紧急回滚

```bash
# 如果重建失败，回滚到之前的状态
BACKUP_DIR="/clawd-data/workspace-backup/20260217-120000"

# 停止服务
openclaw gateway stop
sudo systemctl stop mihomo

# 恢复备份
rm -rf ~/.openclaw/workspace
tar -xzf $BACKUP_DIR/workspace.tar.gz -C ~/.openclaw/

# 重新启动
sudo systemctl start mihomo
openclaw gateway start
```

---

## 联系支持

如果遇到问题：

1. 查看详细文档: https://my.feishu.cn/wiki/QoUCw9iq1ipHwVkHusVcVTYcneg
2. 检查日志文件: `/var/log/bowlwanpi-*.log`
3. 运行诊断脚本: `./scripts/diagnose.sh`

---

**一碗~5分钟重建完成，继续守护你！** 🥣
