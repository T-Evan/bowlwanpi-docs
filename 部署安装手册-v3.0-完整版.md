# 部署安装手册 v3.0 完整版

更新日期: 2026-02-17

---

## 一、系统要求

### 最低配置

| 资源 | 最低要求 | 推荐配置 |
|------|----------|----------|
| **操作系统** | OpenCloudOS 9 / CentOS 8 / Ubuntu 20.04 | OpenCloudOS 9.4 |
| **CPU** | 2核 | 4核+ |
| **内存** | 2GB | 4GB+ |
| **磁盘** | 20GB | 50GB+ SSD |
| **网络** | 可访问互联网 | 海外访问能力 |

### 依赖软件版本

| 软件 | 最低版本 | 推荐版本 |
|------|----------|----------|
| **Node.js** | v20.x | v22.22.0 |
| **Python** | 3.10 | 3.11.6 |
| **Git** | 2.30 | 2.40+ |
| **OpenClaw** | 2026.1.x | 2026.2.6-3 |

---

## 二、系统准备

### 1. 更新系统

```bash
# OpenCloudOS / CentOS
sudo yum update -y
sudo yum install -y git curl wget vim htop net-tools

# Ubuntu
sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl wget vim htop net-tools
```

### 2. 创建数据目录

```bash
# 创建工作区和备份目录
sudo mkdir -p /clawd-data/workspace-backup
sudo mkdir -p /clawd-data/config-backup
sudo chown -R $(whoami):$(whoami) /clawd-data

# 创建日志目录
sudo mkdir -p /var/log/bowlwanpi
sudo chown -R $(whoami):$(whoami) /var/log/bowlwanpi
```

### 3. 配置防火墙

```bash
# 开放OpenClaw端口 (默认18789)
sudo firewall-cmd --permanent --add-port=18789/tcp

# 开放Mihomo端口 (默认7890)
sudo firewall-cmd --permanent --add-port=7890/tcp

# 重新加载防火墙
sudo firewall-cmd --reload

# 查看已开放端口
sudo firewall-cmd --list-ports
```

---

## 三、安装Node.js

### 使用NVM安装 (推荐)

```bash
# 安装NVM
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 加载NVM
export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 安装Node.js v22
nvm install 22
nvm use 22
nvm alias default 22

# 验证安装
node --version  # v22.22.0
npm --version   # 10.x.x
```

### 配置npm镜像 (国内服务器)

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用腾讯云镜像
npm config set registry https://mirrors.cloud.tencent.com/npm/

# 验证配置
npm config get registry
```

---

## 四、安装Python和依赖

### 1. 安装Python 3.11

```bash
# OpenCloudOS / CentOS
sudo yum install -y python3.11 python3.11-pip python3.11-devel

# Ubuntu
sudo apt install -y python3.11 python3.11-pip python3.11-venv python3.11-dev

# 验证安装
python3.11 --version
```

### 2. 安装Python依赖包

```bash
# 升级pip
python3.11 -m pip install --upgrade pip

# 安装核心依赖
python3.11 -m pip install \
  memu-sdk \
  tencentcloud-sdk-python-wsa \
  requests \
  beautifulsoup4 \
  crawl4ai \
  faster-whisper \
  openpyxl \
  pandas \
  numpy

# 验证安装
python3.11 -c "import memu_sdk; print('memu-sdk OK')"
python3.11 -c "import tencentcloud; print('tencentcloud OK')"
```

---

## 五、安装OpenClaw

### 1. 全局安装

```bash
# 安装OpenClaw
npm install -g openclaw

# 验证安装
openclaw --version
```

### 2. 初始化配置

```bash
# 创建配置目录
mkdir -p ~/.openclaw

# 创建基础配置文件
cat > ~/.openclaw/openclaw.json << 'EOF'
{
  "gateway": {
    "port": 18789,
    "auth": {
      "type": "token",
      "token": "your-secure-token-here"
    }
  },
  "plugins": {
    "entries": {}
  },
  "models": {
    "default": "kimi-code/kimi-for-coding",
    "providers": {}
  }
}
EOF

# 验证配置
openclaw config validate
```

---

## 六、安装Mihomo代理

### 1. 下载安装

```bash
# 创建临时目录
mkdir -p /tmp/mihomo-install
cd /tmp/mihomo-install

# 下载最新版Mihomo (根据架构选择)
# x86_64架构
wget https://github.com/MetaCubeX/mihomo/releases/download/v1.18.0/mihomo-linux-amd64-v1.18.0.gz

# ARM64架构
# wget https://github.com/MetaCubeX/mihomo/releases/download/v1.18.0/mihomo-linux-arm64-v1.18.0.gz

# 解压
gunzip mihomo-linux-amd64-v1.18.0.gz

# 移动到系统目录
sudo mv mihomo-linux-amd64-v1.18.0 /usr/local/bin/mihomo
sudo chmod +x /usr/local/bin/mihomo

# 验证安装
mihomo --version
```

### 2. 创建配置目录

```bash
# 创建配置目录
sudo mkdir -p /etc/mihomo

# 创建最小配置文件
sudo tee /etc/mihomo/config.yaml << 'EOF'
mixed-port: 7890
allow-lan: true
bind-address: '*'
mode: rule
log-level: info

# 代理提供者 (需要替换为实际订阅链接)
proxy-providers:
  provider1:
    type: http
    url: "https://your-subscription-url.com/clash"
    interval: 3600
    path: /etc/mihomo/profiles/provider1.yaml
    health-check:
      enable: true
      interval: 600
      url: http://www.gstatic.com/generate_204

proxy-groups:
  - name: "自动选择"
    type: url-test
    use:
      - provider1
    url: http://www.gstatic.com/generate_204
    interval: 300

  - name: "全球直连"
    type: select
    proxies:
      - DIRECT

rules:
  - IP-CIDR,127.0.0.0/8,DIRECT
  - IP-CIDR,172.16.0.0/12,DIRECT
  - IP-CIDR,192.168.0.0/16,DIRECT
  - IP-CIDR,10.0.0.0/8,DIRECT
  - DOMAIN-SUFFIX,cn,DIRECT
  - GEOIP,CN,DIRECT
  - MATCH,自动选择
EOF

# 创建profiles目录
sudo mkdir -p /etc/mihomo/profiles
```

### 3. 创建Systemd服务

```bash
# 创建服务文件
sudo tee /etc/systemd/system/mihomo.service << 'EOF'
[Unit]
Description=Mihomo Proxy Service
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/mihomo -f /etc/mihomo/config.yaml
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

# 重新加载systemd
sudo systemctl daemon-reload

# 启用开机自启
sudo systemctl enable mihomo

# 启动服务
sudo systemctl start mihomo

# 查看状态
sudo systemctl status mihomo
```

### 4. 验证代理

```bash
# 测试代理连接
curl -x http://127.0.0.1:7890 https://www.google.com

# 查看Mihomo日志
sudo journalctl -u mihomo -f
```

---

## 七、部署工作区

### 1. 创建工作区

```bash
# 创建工作区目录
mkdir -p ~/.openclaw/workspace
cd ~/.openclaw/workspace

# 初始化Git仓库
git init

# 创建目录结构
mkdir -p memory scripts secrets skills cron .learnings docs

# 创建.gitignore
cat > .gitignore << 'EOF'
# 凭证文件
secrets/
.env
*.key
*.pem

# 日志文件
*.log

# 临时文件
/tmp/
*.tmp

# 系统文件
.DS_Store
Thumbs.db

# 编辑器配置
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
venv/

# Node.js
node_modules/
package-lock.json
EOF
```

### 2. 配置MCP服务

```bash
# 安装memos-api-mcp
npm install -g @memtensor/memos-api-mcp

# 创建MCP配置文件
cat > mcp_config.json << 'EOF'
{
  "mcpServers": {
    "bowlwanpi": {
      "command": "python3",
      "args": ["mcp_server.py"],
      "env": {
        "HTTP_PROXY": "http://127.0.0.1:7890",
        "HTTPS_PROXY": "http://127.0.0.1:7890"
      }
    },
    "memos-api-mcp": {
      "type": "stdio",
      "command": "/root/.nvm/versions/node/v22.22.0/bin/memos-api-mcp",
      "env": {
        "MEMOS_API_KEY": "your-api-key",
        "MEMOS_USER_ID": "yiwanbot",
        "MEMOS_CHANNEL": "MODELSCOPE"
      }
    },
    "tavily-mcp": {
      "type": "sse",
      "url": "https://mcp.tavily.com/mcp/?tavilyApiKey=your-key"
    }
  }
}
EOF
```

### 3. 配置定时任务

```bash
# 创建cron配置文件
sudo tee /etc/cron.d/bowlwanpi-backup-cron << 'EOF'
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# 每5分钟心跳检查
*/5 * * * * root /root/.openclaw/workspace/scripts/heartbeat.sh >> /var/log/bowlwanpi-heartbeat.log 2>&1

# 每小时Agent新闻监控
0 * * * * root /root/.openclaw/workspace/scripts/agent-news-monitor.py >> /var/log/bowlwanpi-agent-news.log 2>&1

# 夜间构建 (每天凌晨3点)
0 3 * * * root /root/.openclaw/workspace/scripts/nightly-build-enhanced.sh >> /var/log/bowlwanpi-nightly-build.log 2>&1

# 每日任务
0 7 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh morning-prep >> /var/log/bowlwanpi-cron.log 2>&1
0 8 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh one-minute-news >> /var/log/bowlwanpi-cron.log 2>&1
30 8 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh morning-brief >> /var/log/bowlwanpi-cron.log 2>&1
30 8 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh weibo-hot >> /var/log/bowlwanpi-cron.log 2>&1
0 9 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh product-hunt >> /var/log/bowlwanpi-cron.log 2>&1
0 10 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh zhihu-hot >> /var/log/bowlwanpi-cron.log 2>&1
0 12 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh bilibili-hot >> /var/log/bowlwanpi-cron.log 2>&1
0 14 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh info-collect >> /var/log/bowlwanpi-cron.log 2>&1
0 */6 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh github-release >> /var/log/bowlwanpi-cron.log 2>&1
30 22 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh evening-reflect >> /var/log/bowlwanpi-cron.log 2>&1
0 23 * * * root /root/.openclaw/workspace/scripts/backup-cron.sh sleep-reminder >> /var/log/bowlwanpi-cron.log 2>&1

# 周回顾 (每周日晚上8点)
0 20 * * 0 root /root/.openclaw/workspace/scripts/backup-cron.sh weekly-review >> /var/log/bowlwanpi-cron.log 2>&1
EOF

# 设置权限
sudo chmod 644 /etc/cron.d/bowlwanpi-backup-cron

# 重启cron服务
sudo systemctl restart crond

# 验证cron任务
sudo crontab -l
sudo cat /var/log/cron | grep bowlwanpi
```

---

## 八、安装ClawHub技能

### 批量安装命令

```bash
# 基础技能
clawhub install notify session-cost self-improving-agent

# 搜索技能
clawhub install crawl-for-ai

# 媒体技能
clawhub install faster-whisper sag

# 内容监控
clawhub install bilibili-hot-monitor hacker-news

# 娱乐技能
clawhub install ttrpg-gm summarize

# 工具技能
clawhub install task-status weather

# 成本监控
clawhub install openclaw-cost-guard
```

### 验证技能安装

```bash
# 查看已安装技能
clawhub list

# 测试技能
clawhub info notify
```

---

## 九、配置飞书集成

### 1. 创建飞书应用

1. 访问 https://open.feishu.cn/app
2. 点击"创建企业自建应用"
3. 填写应用名称: BowlWanpi
4. 选择应用类型: 企业自建应用

### 2. 获取凭证

在应用详情页:
- **凭证与基础信息** → 获取 App ID 和 App Secret
- **事件订阅** → 设置 Encrypt Key 和 Verification Token
- **权限管理** → 添加以下权限:
  - `im:chat:readonly` (读取群组信息)
  - `im:message.group_msg` (发送群组消息)
  - `im:message:send_as_bot` (以机器人身份发送消息)

### 3. 配置OpenClaw

编辑 `~/.openclaw/openclaw.json`:

```json
{
  "plugins": {
    "entries": {
      "feishu": {
        "enabled": true,
        "appId": "cli_xxxxxxxxxxxxxxxx",
        "appSecret": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
        "encryptKey": "xxxxxxxxxxxxxxxx",
        "verificationToken": "xxxxxxxxxxxx",
        "webhook": {
          "port": 3000,
          "path": "/webhook/feishu"
        }
      }
    }
  }
}
```

### 4. 配置事件订阅

在飞书应用后台:
1. 进入"事件订阅"
2. 设置请求网址: `http://your-server-ip:3000/webhook/feishu`
3. 订阅以下事件:
   - `im.message.receive_v1` (接收消息)

---

## 十、启动服务

### 1. 启动顺序

```bash
# 1. 启动Mihomo代理
sudo systemctl start mihomo

# 2. 验证代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
curl https://www.google.com

# 3. 启动OpenClaw
openclaw gateway start

# 4. 验证OpenClaw
openclaw gateway status

# 5. 验证MCP服务
openclaw mcp list

# 6. 验证定时任务
openclaw cron list
```

### 2. 开机自启设置

```bash
# Mihomo已在安装时设置
sudo systemctl enable mihomo

# 创建OpenClaw服务文件
sudo tee /etc/systemd/system/openclaw.service << 'EOF'
[Unit]
Description=OpenClaw Gateway Service
After=network.target mihomo.service
Wants=mihomo.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/.openclaw/workspace
Environment="HTTP_PROXY=http://127.0.0.1:7890"
Environment="HTTPS_PROXY=http://127.0.0.1:7890"
ExecStart=/root/.nvm/versions/node/v22.22.0/bin/openclaw gateway start
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

# 重新加载systemd
sudo systemctl daemon-reload

# 启用开机自启
sudo systemctl enable openclaw

# 启动服务
sudo systemctl start openclaw

# 查看状态
sudo systemctl status openclaw
```

---

## 十一、验证部署

### 完整验证清单

```bash
#!/bin/bash
# 验证脚本 verify-deployment.sh

echo "=== BowlWanpi 部署验证 ==="

# 1. 检查系统资源
echo -e "\n[1/10] 系统资源"
free -h
df -h

# 2. 检查Node.js
echo -e "\n[2/10] Node.js"
node --version
npm --version

# 3. 检查Python
echo -e "\n[3/10] Python"
python3.11 --version

# 4. 检查OpenClaw
echo -e "\n[4/10] OpenClaw"
openclaw --version

# 5. 检查Mihomo
echo -e "\n[5/10] Mihomo"
mihomo --version
sudo systemctl is-active mihomo

# 6. 检查代理
echo -e "\n[6/10] 代理连接"
curl -x http://127.0.0.1:7890 -s -o /dev/null -w "%{http_code}" https://www.google.com

# 7. 检查OpenClaw配置
echo -e "\n[7/10] OpenClaw配置"
openclaw config validate

# 8. 检查网关状态
echo -e "\n[8/10] 网关状态"
openclaw gateway status

# 9. 检查MCP服务
echo -e "\n[9/10] MCP服务"
openclaw mcp list

# 10. 检查定时任务
echo -e "\n[10/10] 定时任务"
openclaw cron list

echo -e "\n=== 验证完成 ==="
```

---

## 十二、故障排查

### 常见问题

#### 1. OpenClaw无法启动

**症状**: `openclaw gateway start` 失败

**排查步骤**:
```bash
# 检查端口占用
netstat -tlnp | grep 18789

# 检查配置文件
openclaw config validate

# 查看日志
journalctl -u openclaw -f
```

**解决方案**:
- 端口被占用: 修改 `openclaw.json` 中的 `gateway.port`
- 配置错误: 检查JSON格式

#### 2. Mihomo无法连接

**症状**: 代理测试失败

**排查步骤**:
```bash
# 检查服务状态
sudo systemctl status mihomo

# 检查配置文件
mihomo -t -f /etc/mihomo/config.yaml

# 查看日志
sudo journalctl -u mihomo -f
```

**解决方案**:
- 订阅链接失效: 更新 `proxy-providers.url`
- 配置错误: 检查YAML格式

#### 3. 定时任务不执行

**症状**: cron任务没有运行

**排查步骤**:
```bash
# 检查cron服务
sudo systemctl status crond

# 检查cron配置
sudo cat /etc/cron.d/bowlwanpi-backup-cron

# 查看cron日志
sudo cat /var/log/cron | grep bowlwanpi
```

**解决方案**:
- 权限问题: `sudo chmod 644 /etc/cron.d/bowlwanpi-backup-cron`
- 路径错误: 检查脚本路径是否正确
- 环境变量: 在cron中设置完整PATH

#### 4. MCP服务连接失败

**症状**: `openclaw mcp list` 显示离线

**排查步骤**:
```bash
# 检查MCP配置
cat ~/.openclaw/workspace/mcp_config.json | python3 -m json.tool

# 测试MCP服务
# bowlwanpi
python3 ~/.openclaw/workspace/mcp_server.py --test

# memos-api-mcp
/root/.nvm/versions/node/v22.22.0/bin/memos-api-mcp --version
```

**解决方案**:
- 路径错误: 检查 `command` 和 `args` 路径
- 权限问题: 检查文件执行权限
- 依赖缺失: 安装所需依赖包

---

## 十三、备份与恢复

### 自动备份脚本

```bash
#!/bin/bash
# 备份脚本 backup.sh

BACKUP_DIR="/clawd-data/workspace-backup/$(date +%Y%m%d-%H%M%S)"
mkdir -p $BACKUP_DIR

echo "开始备份到: $BACKUP_DIR"

# 备份工作区
tar -czf $BACKUP_DIR/workspace.tar.gz \
  --exclude='node_modules' \
  --exclude='__pycache__' \
  --exclude='.git' \
  -C ~/.openclaw workspace/

# 备份配置
cp ~/.openclaw/openclaw.json $BACKUP_DIR/
cp ~/.openclaw/workspace/mcp_config.json $BACKUP_DIR/

# 备份凭证 (加密)
tar -czf $BACKUP_DIR/secrets.tar.gz -C ~/.openclaw/workspace secrets/

# 备份Mihomo配置
cp /etc/mihomo/config.yaml $BACKUP_DIR/

# 清理旧备份 (保留最近10个)
ls -1td /clawd-data/workspace-backup/*/ | tail -n +11 | xargs rm -rf

echo "备份完成: $BACKUP_DIR"
```

### 恢复脚本

```bash
#!/bin/bash
# 恢复脚本 restore.sh

BACKUP_DIR=$1

if [ -z "$BACKUP_DIR" ]; then
  echo "用法: $0 <备份目录>"
  exit 1
fi

echo "从 $BACKUP_DIR 恢复..."

# 恢复工作区
tar -xzf $BACKUP_DIR/workspace.tar.gz -C ~/.openclaw/

# 恢复配置
cp $BACKUP_DIR/openclaw.json ~/.openclaw/
cp $BACKUP_DIR/mcp_config.json ~/.openclaw/workspace/

# 恢复凭证
tar -xzf $BACKUP_DIR/secrets.tar.gz -C ~/.openclaw/workspace/

# 恢复Mihomo配置
sudo cp $BACKUP_DIR/config.yaml /etc/mihomo/

echo "恢复完成"
```

---

## 十四、更新升级

### OpenClaw更新

```bash
# 更新OpenClaw
npm update -g openclaw

# 验证版本
openclaw --version
```

### 技能更新

```bash
# 更新所有ClawHub技能
clawhub update

# 或单独更新
clawhub update notify
clawhub update crawl-for-ai
```

### 系统更新

```bash
# 更新系统包
sudo yum update -y  # CentOS/OpenCloudOS
sudo apt update && sudo apt upgrade -y  # Ubuntu

# 更新Python包
python3.11 -m pip list --outdated
python3.11 -m pip install --upgrade <package-name>
```

---

一碗~部署完成！开始享受你的AI助手吧！🥣
