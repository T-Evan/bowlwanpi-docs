# 配置文件详解 v3.0 完整版

更新日期: 2026-02-17

---

## 一、凭证文件清单

所有凭证文件都存放在 `~/.openclaw/workspace/secrets/` 目录下，**请勿提交到git仓库！**

### 1. memu-credentials.json (memU云端记忆)

**文件位置**: `secrets/memu-credentials.json`

**用途**: 连接memU云端记忆系统

**内容示例**:
```json
{
  "api_key": "your-memu-api-key-here",
  "api_endpoint": "https://api.memu.so",
  "user_id": "yiwan",
  "agent_id": "bowlwanpi"
}
```

**获取方式**:
1. 访问 https://memu.so 注册账号
2. 创建新的Agent
3. 复制API Key

---

### 2. tencent-credentials.json (腾讯云WSA搜索)

**文件位置**: `secrets/tencent-credentials.json`

**用途**: 腾讯云WSA搜索服务认证

**内容示例**:
```json
{
  "secret_id": "your-secret-id",
  "secret_key": "your-secret-key",
  "region": "ap-beijing"
}
```

**获取方式**:
1. 登录腾讯云控制台
2. 进入"访问管理" → "API密钥管理"
3. 创建新的密钥对

---

### 3. moltbook-credentials.json (Moltbook社区)

**文件位置**: `secrets/moltbook-credentials.json`

**用途**: Moltbook AI社区API认证

**内容示例**:
```json
{
  "agent_id": "dfc4fae2-ac13-4124-9bfe-6d12da5ec72f",
  "api_key": "moltbook_sk_PM-M3yRSJ3B98JgYENp8m3EFjHy6RW0X",
  "api_endpoint": "https://moltbook.com/api/v1"
}
```

**获取方式**:
1. 访问 https://moltbook.com
2. 注册Agent账号
3. 在设置页面获取API Key

---

### 4. tavily-credentials.env (Tavily搜索)

**文件位置**: `secrets/tavily-credentials.env`

**用途**: Tavily AI搜索API认证

**内容示例**:
```bash
TAVILY_API_KEY=tvly-your-api-key-here
```

**获取方式**:
1. 访问 https://tavily.com
2. 注册账号
3. 在Dashboard获取API Key

---

### 5. doubao-tts/.env (豆包语音合成)

**文件位置**: `skills/doubao-tts/.env`

**用途**: 豆包(火山引擎)语音合成服务

**内容示例**:
```bash
VOLCENGINE_APP_ID=your-app-id
VOLCENGINE_ACCESS_TOKEN=your-access-token
VOLCENGINE_CLUSTER_ID=your-cluster-id
DEFAULT_VOICE=zh_female_cancan_mars_bigtts
```

**获取方式**:
1. 访问 https://console.volcengine.com
2. 开通"语音合成"服务
3. 创建应用获取AppID和Token

---

## 二、openclaw.json 配置详解

**文件位置**: `~/.openclaw/openclaw.json`

### 完整配置示例

```json
{
  "gateway": {
    "port": 18789,
    "auth": {
      "type": "token",
      "token": "your-secure-token-here"
    },
    "cors": {
      "enabled": true,
      "origins": ["*"]
    }
  },
  "plugins": {
    "entries": {
      "feishu": {
        "enabled": true,
        "appId": "cli_xxxxxxxxxxxxxxxx",
        "appSecret": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
        "encryptKey": "xxxxxxxxxxxxxxxx",
        "verificationToken": "xxxxxxxxxxxx"
      }
    }
  },
  "models": {
    "default": "kimi-code/kimi-for-coding",
    "subagent": "crs/gpt-5.3-codex",
    "providers": {
      "kimi-code": {
        "baseUrl": "https://api.kimi.com/coding/v1",
        "apiKey": "your-kimi-api-key",
        "timeout": 120
      },
      "crs": {
        "baseUrl": "https://api.coding.ai/v1",
        "apiKey": "your-crs-api-key",
        "timeout": 120
      },
      "minimax-portal": {
        "baseUrl": "https://api.minimax.chat/v1",
        "apiKey": "your-minimax-api-key"
      }
    }
  },
  "logging": {
    "level": "info",
    "format": "json",
    "output": "/var/log/openclaw.log"
  },
  "features": {
    "autoSave": true,
    "autoSaveInterval": 300,
    "maxHistory": 100,
    "enableThinking": true
  }
}
```

### 关键配置项说明

#### Gateway配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `gateway.port` | number | 18789 | OpenClaw网关服务端口 |
| `gateway.auth.type` | string | "token" | 认证类型: token/none |
| `gateway.auth.token` | string | - | 访问令牌 |
| `gateway.cors.enabled` | boolean | true | 是否启用CORS |

#### 模型配置

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `models.default` | string | - | 默认使用的模型 |
| `models.subagent` | string | - | 子代理使用的模型 |
| `models.providers` | object | - | 各模型提供商配置 |

#### Feishu插件配置

| 配置项 | 类型 | 说明 |
|--------|------|------|
| `plugins.feishu.enabled` | boolean | 是否启用飞书插件 |
| `plugins.feishu.appId` | string | 飞书应用ID |
| `plugins.feishu.appSecret` | string | 飞书应用密钥 |
| `plugins.feishu.encryptKey` | string | 加密密钥 |
| `plugins.feishu.verificationToken` | string | 验证令牌 |

**获取方式**:
1. 访问 https://open.feishu.cn/app
2. 创建企业自建应用
3. 在"凭证与基础信息"中获取AppID和Secret
4. 在"事件订阅"中配置EncryptKey和VerificationToken

---

## 三、mcp_config.json 配置详解

**文件位置**: `~/.openclaw/workspace/mcp_config.json`

### 完整配置示例

```json
{
  "mcpServers": {
    "bowlwanpi": {
      "command": "python3",
      "args": [
        "/root/.openclaw/workspace/mcp_server.py"
      ],
      "env": {
        "HTTP_PROXY": "http://127.0.0.1:7890",
        "HTTPS_PROXY": "http://127.0.0.1:7890",
        "MEMU_API_KEY": "${secrets.memu.api_key}",
        "TENCENT_SECRET_ID": "${secrets.tencent.secret_id}",
        "TENCENT_SECRET_KEY": "${secrets.tencent.secret_key}"
      },
      "cwd": "/root/.openclaw/workspace",
      "timeout": 60
    },
    "memos-api-mcp": {
      "type": "stdio",
      "command": "/root/.nvm/versions/node/v22.22.0/bin/memos-api-mcp",
      "args": [],
      "env": {
        "MEMOS_API_KEY": "your-api-key",
        "MEMOS_USER_ID": "yiwanbot",
        "MEMOS_CHANNEL": "MODELSCOPE",
        "MEMOS_ENDPOINT": "https://api.memos.so"
      },
      "timeout": 60
    },
    "tavily-mcp": {
      "type": "sse",
      "url": "https://mcp.tavily.com/mcp/?tavilyApiKey=your-api-key",
      "headers": {
        "Authorization": "Bearer your-api-key"
      },
      "timeout": 30
    }
  }
}
```

### MCP服务类型说明

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| **stdio** | 标准输入输出通信 | 本地脚本、Node模块 |
| **sse** | Server-Sent Events | 远程服务、流式响应 |
| **http** | HTTP REST API | 通用API服务 |

### bowlwanpi MCP服务工具列表

| 工具名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `get_system_status` | 获取系统状态 | 无 | CPU/内存/磁盘状态 |
| `search_memory` | 检索记忆 | query, limit | 记忆列表 |
| `remember_fact` | 存储记忆 | text, category, importance | 存储结果 |
| `get_daily_schedule` | 获取定时任务 | 无 | 任务列表 |
| `get_moltbook_info` | 获取Moltbook信息 | 无 | 账号信息 |

---

## 四、Mihomo代理配置详解

**文件位置**: `/etc/mihomo/config.yaml` 或 `~/.config/mihomo/config.yaml`

### 最小可用配置

```yaml
# 混合端口 (HTTP/HTTPS/SOCKS5统一端口)
mixed-port: 7890

# 允许局域网连接
allow-lan: true
bind-address: '*'

# 模式: rule(规则模式)/global(全局模式)/direct(直连模式)
mode: rule

# 日志级别: debug/info/warning/error/silent
log-level: info

# 代理提供者 (订阅链接)
proxy-providers:
  provider1:
    type: http
    url: "https://your-subscription-url.com/clash"
    interval: 3600
    path: ./profiles/provider1.yaml
    health-check:
      enable: true
      interval: 600
      url: http://www.gstatic.com/generate_204

# 代理组
proxy-groups:
  - name: "自动选择"
    type: url-test
    use:
      - provider1
    url: http://www.gstatic.com/generate_204
    interval: 300
    tolerance: 50

  - name: "手动选择"
    type: select
    use:
      - provider1

  - name: "全球直连"
    type: select
    proxies:
      - DIRECT

# 路由规则
rules:
  # 局域网直连
  - IP-CIDR,127.0.0.0/8,DIRECT
  - IP-CIDR,172.16.0.0/12,DIRECT
  - IP-CIDR,192.168.0.0/16,DIRECT
  - IP-CIDR,10.0.0.0/8,DIRECT

  # 国内域名直连
  - DOMAIN-SUFFIX,cn,DIRECT
  - DOMAIN-SUFFIX,baidu.com,DIRECT
  - DOMAIN-SUFFIX,tencent.com,DIRECT
  - DOMAIN-SUFFIX,aliyun.com,DIRECT

  # 国内IP直连
  - GEOIP,CN,DIRECT

  # 其他走代理
  - MATCH,自动选择
```

### 配置项说明

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `mixed-port` | number | 7890 | HTTP/HTTPS/SOCKS5统一端口 |
| `allow-lan` | boolean | false | 允许局域网连接 |
| `mode` | string | rule | 运行模式 |
| `log-level` | string | info | 日志级别 |

### 代理组类型

| 类型 | 说明 | 使用场景 |
|------|------|----------|
| `select` | 手动选择 | 需要手动切换节点 |
| `url-test` | 自动测速选择 | 自动选择延迟最低的节点 |
| `load-balance` | 负载均衡 | 流量分散到多个节点 |
| `fallback` | 故障转移 | 主节点故障时自动切换 |

---

## 五、环境变量配置

### 必需的环境变量

```bash
# 添加到 ~/.bashrc 或 ~/.zshrc

# Mihomo代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
export ALL_PROXY=socks5://127.0.0.1:7890

# Node.js路径
export PATH="/root/.nvm/versions/node/v22.22.0/bin:$PATH"

# Python路径 (如果使用特定版本)
export PATH="/usr/local/python3.11/bin:$PATH"

# OpenClaw配置
export OPENCLAW_CONFIG=/root/.openclaw/openclaw.json
export OPENCLAW_WORKSPACE=/root/.openclaw/workspace

# 时区设置
export TZ=Asia/Shanghai
```

### 应用环境变量

```bash
# 立即生效
source ~/.bashrc

# 验证代理
env | grep -i proxy

# 验证Node路径
which node
node --version
```

---

## 六、Git配置

**文件位置**: `~/.openclaw/workspace/.git/config`

### 推荐配置

```ini
[core]
    repositoryformatversion = 0
    filemode = true
    bare = false
    logallrefupdates = true
    # 忽略文件权限变化
    fileMode = false

[remote "origin"]
    url = https://github.com/yourusername/bowlwanpi-workspace.git
    fetch = +refs/heads/*:refs/remotes/origin/*

[branch "main"]
    remote = origin
    merge = refs/heads/main

[user]
    name = BowlWanpi
    email = bowlwanpi@example.com

[credential]
    helper = store

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
```

---

## 七、Systemd服务配置

### Mihomo服务

**文件位置**: `/etc/systemd/system/mihomo.service`

```ini
[Unit]
Description=Mihomo Proxy Service
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/mihomo -f /etc/mihomo/config.yaml
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 启动命令

```bash
# 重新加载systemd配置
sudo systemctl daemon-reload

# 启用开机自启
sudo systemctl enable mihomo

# 启动服务
sudo systemctl start mihomo

# 查看状态
sudo systemctl status mihomo

# 查看日志
sudo journalctl -u mihomo -f
```

---

## 八、验证配置

### 配置验证命令

```bash
# 验证OpenClaw配置
openclaw config validate

# 检查网关状态
openclaw gateway status

# 列出MCP服务
openclaw mcp list

# 列出定时任务
openclaw cron list

# 测试代理连接
curl -x http://127.0.0.1:7890 https://www.google.com

# 验证Mihomo配置
mihomo -t -f /etc/mihomo/config.yaml
```

### 常见问题排查

| 问题 | 检查项 | 解决方案 |
|------|--------|----------|
| 配置验证失败 | JSON格式错误 | 使用JSON校验工具检查 |
| MCP连接失败 | 配置文件路径 | 确认路径和权限正确 |
| 代理不工作 | Mihomo状态 | 检查服务是否运行 |
| API认证失败 | 凭证文件 | 检查API Key是否过期 |

---

## 九、配置备份建议

### 需要备份的文件

```bash
# 创建备份脚本 backup-config.sh
#!/bin/bash
BACKUP_DIR="/clawd-data/config-backup/$(date +%Y%m%d)"
mkdir -p $BACKUP_DIR

# 备份OpenClaw配置
cp ~/.openclaw/openclaw.json $BACKUP_DIR/

# 备份MCP配置
cp ~/.openclaw/workspace/mcp_config.json $BACKUP_DIR/

# 备份凭证文件(加密存储)
cp -r ~/.openclaw/workspace/secrets $BACKUP_DIR/

# 备份Mihomo配置
cp /etc/mihomo/config.yaml $BACKUP_DIR/

# 备份技能环境变量
find ~/.openclaw/workspace/skills -name ".env" -exec cp {} $BACKUP_DIR/ \;

echo "配置已备份到: $BACKUP_DIR"
```

---

一碗~配置详解完成！记得保管好你的凭证文件哦！🥣
