# 🔧 碗皮运维手册 v1.0

**文档版本:** 1.0  
**最后更新:** 2026-02-14  
**适用范围:** BowlWanpi 系统运维

---

## 📋 目录

1. [系统架构](#系统架构)
2. [日常运维](#日常运维)
3. [故障排查](#故障排查)
4. [应急响应](#应急响应)
5. [常用命令](#常用命令)

---

## 系统架构

### 核心组件

```
┌─────────────────────────────────────────────────────────┐
│                    一碗 (Human)                          │
└──────────────────────┬──────────────────────────────────┘
                       │ 飞书消息
                       ▼
┌─────────────────────────────────────────────────────────┐
│              OpenClaw Gateway                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 消息路由  │  │ 技能调度  │  │ 会话管理  │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│                   BowlWanpi (碗皮)                       │
│  ┌───────────────────────────────────────────────────┐ │
│  │              核心系统                              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │ │
│  │  │心跳监控   │ │安全扫描   │ │定时任务   │          │ │
│  │  └──────────┘ └──────────┘ └──────────┘          │ │
│  └───────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────┐ │
│  │              记忆系统                              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │ │
│  │  │QMDR      │ │memU      │ │Hippocampus│          │ │
│  │  └──────────┘ └──────────┘ └──────────┘          │ │
│  │  ┌──────────┐                                     │ │
│  │  │MemOS     │                                     │ │
│  │  └──────────┘                                     │ │
│  └───────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────┐ │
│  │              外部服务                              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │ │
│  │  │Moltbook  │ │飞书      │ │Mihomo    │          │ │
│  │  └──────────┘ └──────────┘ └──────────┘          │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 关键路径

1. **消息流入**: OpenClaw Gateway → 碗皮处理 → 回复一碗
2. **定时任务**: Cron → 脚本执行 → 飞书推送
3. **记忆同步**: 对话 → 四记忆系统 → 云端存储
4. **监控告警**: 心跳检查 → 异常检测 → 飞书通知

---

## 日常运维

### 每日检查清单

- [ ] Gateway 运行状态
- [ ] Mihomo 代理状态
- [ ] 定时任务执行情况
- [ ] 磁盘空间使用率
- [ ] 内存使用率

### 每周检查清单

- [ ] 安全扫描 (`bash scripts/security_check.sh`)
- [ ] 备份验证
- [ ] 回滚点创建 (`bash scripts/rollback_manager.sh create`)
- [ ] 日志清理
- [ ] 系统更新检查

### 每月检查清单

- [ ] API Key 轮换
- [ ] 依赖更新检查
- [ ] 性能基准测试
- [ ] 文档更新
- [ ] 灾难恢复演练

---

## 故障排查

### 问题分类

#### 🔴 Critical (立即处理)

| 症状 | 可能原因 | 解决方案 |
|------|----------|----------|
| Gateway 无响应 | 进程崩溃 | `openclaw gateway restart` |
| 内存使用率 > 90% | 内存泄漏 | 重启相关服务 |
| 磁盘满 | 日志过多 | 清理日志文件 |
| 无法连接飞书 | 网络问题 | 检查 Mihomo 代理 |

#### 🟡 Warning (需要关注)

| 症状 | 可能原因 | 解决方案 |
|------|----------|----------|
| CPU 使用率 > 80% | 高负载任务 | 检查定时任务 |
| 定时任务失败 | 脚本错误 | 查看日志分析 |
| 记忆同步延迟 | 网络问题 | 检查 API 状态 |
| Mihomo 未运行 | 配置错误 | 检查配置文件 |

#### ℹ️ Info (观察即可)

| 症状 | 说明 |
|------|------|
| 告警频率增加 | 可能是临时波动 |
| 日志文件增大 | 正常增长 |
| 脚本执行时间增加 | 数据量增加导致 |

### 排查流程

```
发现问题
    ↓
查看心跳日志: tail -f /var/log/bowlwanpi-heartbeat.log
    ↓
判断问题级别
    ↓
┌──────────────┬──────────────┬──────────────┐
│   Critical   │   Warning    │     Info     │
├──────────────┼──────────────┼──────────────┤
│ 立即处理      │ 记录并观察    │ 正常处理      │
│ 通知一碗      │ 准备预案      │ 继续监控      │
└──────────────┴──────────────┴──────────────┘
    ↓
解决问题
    ↓
验证修复
    ↓
记录事件
```

### 常见问题速查

#### 1. Gateway 无法启动
```bash
# 检查日志
openclaw gateway logs

# 检查端口占用
lsof -i :8080

# 重启 Gateway
openclaw gateway restart
```

#### 2. 定时任务不执行
```bash
# 检查 cron 状态
systemctl status cron

# 查看 cron 日志
grep CRON /var/log/syslog | tail -20

# 手动执行任务测试
bash scripts/push_weibo_hot.py
```

#### 3. 记忆同步失败
```bash
# 检查 API 状态
curl -s https://api.memu.so/health

# 检查网络
ping -c 3 api.memu.so

# 查看同步日志
tail -f ~/.openclaw/workspace/logs/sync.log
```

#### 4. 飞书消息发送失败
```bash
# 检查 webhook
bash scripts/heartbeat_v3.sh

# 检查代理
curl -x http://127.0.0.1:7890 https://www.feishu.cn

# 检查 token
python3 scripts/send-feishu.py --test
```

---

## 应急响应

### 响应级别

#### Level 1: 服务完全中断
- **触发条件**: Gateway 无法启动 + 无法手动恢复
- **响应时间**: 立即
- **处理步骤**:
  1. 通知一碗
  2. 检查系统资源
  3. 尝试重启服务
  4. 如无法恢复，使用回滚点
  5. 记录事件详情

#### Level 2: 功能部分受损
- **触发条件**: 部分定时任务失败或响应变慢
- **响应时间**: 30 分钟内
- **处理步骤**:
  1. 查看错误日志
  2. 隔离问题组件
  3. 手动修复或重启
  4. 监控恢复情况

#### Level 3: 轻微异常
- **触发条件**: 偶发告警，不影响主要功能
- **响应时间**: 下次维护时处理
- **处理步骤**:
  1. 记录问题
  2. 分析原因
  3. 计划修复

### 回滚操作

```bash
# 1. 查看可用回滚点
bash scripts/rollback_manager.sh list

# 2. 创建当前状态回滚点（防止回滚失败）
bash scripts/rollback_manager.sh create "before_emergency_rollback"

# 3. 执行回滚
bash scripts/rollback_manager.sh rollback <回滚点名称>

# 4. 验证系统状态
bash scripts/heartbeat_v3.sh

# 5. 通知一碗
```

### 联系信息

- **一碗**: 飞书消息
- **OpenClaw 支持**: https://docs.openclaw.ai
- **Moltbook 社区**: https://www.moltbook.com

---

## 常用命令

### 监控命令

```bash
# 查看心跳状态
bash scripts/heartbeat_v3.sh

# 查看安全状态
bash scripts/security_status.sh

# 查看系统资源
top
free -h
df -h
```

### 维护命令

```bash
# 创建回滚点
bash scripts/rollback_manager.sh create "$(date +%Y%m%d)_maintenance"

# 执行安全扫描
bash scripts/security_check.sh

# 清理日志
find /var/log -name "*.log" -mtime +7 -delete

# 备份 workspace
bash scripts/backup.sh
```

### 调试命令

```bash
# 测试飞书消息
bash scripts/send-feishu.py --test

# 测试 Moltbook API
python3 scripts/moltbook_client.py --test

# 测试记忆同步
python3 scripts/batch_store_history.py --dry-run
```

---

## 附录

### A. 关键文件位置

| 文件/目录 | 位置 | 说明 |
|-----------|------|------|
| 工作目录 | ~/.openclaw/workspace | 主工作区 |
| 脚本目录 | scripts/ | 所有脚本 |
| 学习笔记 | learning/ | Moltbook 学习资料 |
| 安全配置 | SECURITY.md | 安全最佳实践 |
| 回滚点 | .rollbacks/ | 自动备份 |
| 心跳日志 | /var/log/bowlwanpi-heartbeat.log | 监控日志 |
| 告警日志 | /var/log/bowlwanpi-alerts.log | 告警记录 |
| Secrets | secrets/ | API 密钥等 |

### B. 定时任务清单

| 任务 | 频率 | 脚本 |
|------|------|------|
| 心跳监控 | 每 5 分钟 | heartbeat_v3.sh |
| 夜间构建 | 每天 3:00 | nightly_build_v3.sh |
| 安全扫描 | 每周日 3:00 | security_cron.sh |
| 记忆同步 | 每小时 | batch_store_history.py |
| 系统备份 | 每 4 小时 | backup.sh |

### C. 性能基准

| 指标 | 正常范围 | 告警阈值 |
|------|----------|----------|
| CPU | 0-60% | > 80% |
| 内存 | 0-70% | > 90% |
| 磁盘 | 0-80% | > 90% |
| 响应时间 | < 2s | > 5s |

---

*维护: BowlWanpi 🥣*  
*最后更新: 2026-02-14*  
*版本: 1.0*